# Optional V2-B / V2-D stack. Install only after the V2-A backend is validated.
-r requirements.txt

# GPU / inference
# Install the CUDA-compatible PyTorch build appropriate for the machine first.
torch>=2.4
 torchaudio>=2.4

# Audio ML layers planned for LMNotebook
transformers>=4.45
accelerate>=1.0
faster-whisper>=1.1
demucs>=4.0
librosa>=0.10
soundfile>=0.12
numpy>=1.26
scipy>=1.13
