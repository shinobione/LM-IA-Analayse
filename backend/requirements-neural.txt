# V2-B — optional neural music layer.
# Kept deliberately separate from the stable V2-A runtime.
# PyTorch CUDA is installed explicitly by LMNotebook_START.cmd from the
# official PyTorch cu128 wheel index; do not add torch here.

transformers>=5.0,<6
numpy>=2.0,<3
librosa>=0.11,<1
safetensors>=0.6,<1
huggingface-hub>=0.34,<2
