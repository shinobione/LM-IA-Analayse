# V2-D — optional GPU stem separation layer.
# IMPORTANT: this file is installed only into backend/.venv-stems.
# PyTorch + torchaudio CUDA are installed first and pinned explicitly by the
# launcher; install this file WITHOUT --upgrade so Demucs cannot replace them.
# V2-A/V2-B live in backend/.venv and must never share Demucs dependencies.

numpy>=1.26,<3
demucs==4.1.0
